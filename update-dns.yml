hosts: localhost
connection: local
gather_facts: true

tasks:
  - name: Update DNS
    route53:
      state: present
      zone: "{{ zone }}"
      record: "{{ hostname }}.{{ zone }}"
      type: A
      ttl: 300
      value: "{{ ansible_default_ipv4.address }}"

